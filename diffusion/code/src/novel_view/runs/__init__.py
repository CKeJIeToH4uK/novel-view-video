"""Operational records of :mod:`novel_view` attempts.

Owns only the portable ``attempt.json`` written by the Python runner.  It
uses the standard library, does not import jobs or workflows and does not own
legacy scientific ``run.json`` formats.
"""
