"""Concrete EUVS inputs over the nuPlan ingress.

Owns the external ordered selection, frame index, physical pair and
rasterized EUVS sequences.
May import ``config.load``, ``inputs.types``, ``inputs.nuplan`` and
``geometry``.
Does not own recipes, alignment, generation or evaluation.
"""
