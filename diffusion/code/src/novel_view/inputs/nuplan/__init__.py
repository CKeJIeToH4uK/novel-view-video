"""nuPlan SQLite, camera and stored-raster ingress.

The package owns raw nuPlan decoding and its direct conversion to physical
camera and pinhole raster data.  It may depend on ``novel_view.geometry`` and
does not re-export or eagerly import OpenCV.
"""
