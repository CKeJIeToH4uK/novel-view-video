"""Явные диагностики внешних ресурсов проекта.

Владеет добровольными проверками training topology, Gen3C model-ready,
Waymo ingress/raster и установленного образа без GPU (`image.py`).
`job.py` по явному doctor читает только входы и веса выбранной задачи;
`_job_worker.py` проверяет её backend в нужном изолированном Python-prefix.
`waymo.py` доступен через явные `./distil3d doctor waymo-ingress|waymo-raster`:
фиксированное exposed-debug окно и старые пороги сохранены; job не нужен.
Может импортировать нижние production-реализации только
после прямого выбора конкретной диагностики. Обычные CLI, plan и workflows
эту область не импортируют. Пакет не выполняет тяжёлых импортов и ничего не
переэкспортирует.
"""
