"""Explicit versioned workflow compositions.

Owns lazy routing by ``(name, version)`` and visible scientific call order.
It does not provide a base class, plugin registry or arbitrary YAML stages.
"""
