# Gaussian selections

Пять файлов задают открытые малые формы selection v1 для workflow v1–v5.
`scene_id: 0` и малые IDs являются заменяемым примером, а не закрытой scene;
реальная кампания использует согласованные campaign-local копии.
