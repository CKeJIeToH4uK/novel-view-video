# Пример выбора checkpoint v1

`run.yaml` показывает строгую форму `gen3c_checkpoint_selection/v1`.
Скопируйте папку в `jobs/local/` и замените оба примера путей точными JSON-
записями фактически завершённых попыток. Каталоги не сканируются, `latest`
не используется.
