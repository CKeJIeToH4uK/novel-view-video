# Пример выбора checkpoint v2

`run.yaml` показывает строгую форму `gen3c_checkpoint_selection/v2`.
Локальная копия должна перечислять только JSON-записи одной objective и
одного observer lineage; пути заменяются после реальных запусков.
