# Исправленное решение по глубине

`run.yaml` применяет gate v2 к тем же восьми ordered candidate records.
Количество heldout-точек считается общим свойством измеренного LiDAR, а не
разным свойством MoGe и VGGT. Результат пишется в новую схему v4 без Git- и
SHA-проверок.
